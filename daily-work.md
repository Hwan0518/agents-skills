---
description: 선택한 커밋과 PR 내용을 다른 사람이 이해할 수 있는 일일 업무 기록으로 변환하고 커밋별 Markdown 파일로 저장한다
allowed-tools: Bash, Read, Grep, Glob, Write, Edit, AskUserQuestion
---

# /daily-work — 커밋별 일일 업무 기록 작성

선택한 커밋을 다른 사람도 이해할 수 있는 일일 업무 기록으로 변환하고 날짜별로 저장한다.
모든 사용자 응답과 산출물은 한국어로 작성한다.

인자: `$ARGUMENTS` — 기록 날짜(`YYYY-MM-DD`), 커밋 해시·개수, 추가 지시를 선택적으로 전달

## 핵심 원칙

- PR은 전체 작업의 맥락을 제공하는 보조 근거로 사용한다.
- 일일 업무 기록의 기본 단위는 개별 커밋으로 고정한다.
- 선택한 커밋 1개당 Markdown 파일 1개를 생성한다.
- 같은 PR에 포함됐다는 이유로 서로 다른 커밋을 하나의 설명이나 파일로 합치지 않는다.
- 실제 변경 사실은 커밋 diff를 기준으로 판단한다.
- 커밋 내용만으로 알 수 없는 목적과 정책은 추측하지 않고 사용자에게 확인한다.
- 파일을 쓰기 전에 전체 초안을 보여주고 사용자 승인을 받는다.

## 입력

- 기록 날짜
  - 형식: `YYYY-MM-DD`
  - 누락 시 오늘로 가정하지 않고 사용자에게 확인한다.
- 대상 커밋
  - 커밋 해시 목록 또는 최근 커밋 개수
  - 누락 시 기록 날짜와 현재 브랜치를 기준으로 후보를 조회해 사용자에게 선택받는다.
- PR 내용
  - 사용자가 제공한 본문을 우선 사용한다.
  - 제공되지 않았으면 현재 브랜치의 PR을 `gh pr view`로 읽어도 된다.
  - PR을 찾지 못하면 본문을 받을지, 커밋만으로 진행할지 확인한다.
- 추가 지시
  - 독자 수준, 제외할 내용, 파일명 등 사용자가 명시한 조건

## 근거 우선순위

1. 실제 커밋 diff
2. 커밋 메시지
3. PR의 Summary와 Decisions
4. 관련 설계 문서, 도메인 위키, 프로젝트 규칙
5. 사용자에게 확인받은 업무 배경

- PR 본문을 실제 변경 사실의 근거로 단독 사용하지 않는다.
- PR의 공통 설명을 각 커밋에 그대로 반복하지 않는다.
- PR의 내용은 관련된 커밋의 배경과 의사결정을 보완하는 데만 사용한다.

## 실행 절차

### 1. 기록 날짜와 커밋 확정

- 저장소 루트에서 작업한다.
- `git status --short`로 현재 변경 상태를 확인하되 기존 변경을 수정하거나 정리하지 않는다.
- `git log`로 커밋 해시, 작성 시각, 제목, 본문을 확인한다.
- 사용자가 최근 N개를 지정하면 해당 커밋을 조회해 목록을 보여준다.
- 사용자가 커밋을 지정하지 않으면 기록 날짜의 커밋 후보를 보여주고 선택받는다.
- 병합 커밋이나 다른 사람의 커밋이 섞여 있으면 명시적으로 표시한다.
- 대상 커밋을 확정하기 전에는 초안 작성을 시작하지 않는다.

### 2. 커밋별 실제 변경 확인

- 각 커밋마다 `git show --stat`, `git diff-tree`, 실제 diff를 확인한다.
- 필요하면 변경된 핵심 파일과 관련 규칙·설계 문서를 직접 읽는다.
- 파일명, 커밋 제목, 주석만으로 동작이나 목적을 추측하지 않는다.
- 각 커밋에 대해 아래 항목을 내부적으로 정리한다.
  - 한 줄 책임
  - 실제 작업 내용
  - 문제와 의사결정 배경
  - 변경 전후의 업무 또는 개발 흐름
  - 설명이 필요한 도메인·기술 개념
  - 선택한 방식과 대안·제약
  - 2단계 설명 후보
  - 미확인 사항

### 3. 불명확한 내용 확인

다음 조건에서는 사용자 답을 받은 후 진행한다.

- 커밋 diff만으로 변경 이유를 확인할 수 없는 경우
- PR 설명과 실제 변경이 일치하지 않는 경우
- 한 커밋에 서로 무관한 책임이 섞여 있는 경우
- 업무 정책과 단순 구현 선택의 경계가 불명확한 경우
- 기대 효과를 근거 없이 추측해야 하는 경우
- 2단계에서 공유할 판단의 배경이 확인되지 않은 경우

- 코드나 저장소 문서에서 확인 가능한 내용은 질문하지 않고 직접 확인한다.
- 질문은 작성에 필요한 순서대로 하나씩 한다.
- 답이 불명확하면 임의로 해석하지 않고 다시 확인한다.

### 4. 1단계(default) 작성

- 모든 커밋에 반드시 작성한다.
- 파일이나 클래스 목록보다 의사결정, 배경, 책임, 흐름을 설명한다.
- 아래 세 항목을 고정 형식으로 사용한다.
  - 작업내용
  - 왜 그 작업을 했는지
  - 배경지식(도메인) 설명

#### 작성 기준

- 작업내용
  - 어떤 책임이나 흐름을 만들거나 변경했는지 설명
  - 세부 파일 나열보다 변경의 의미를 우선
- 왜 그 작업을 했는지
  - 기존 문제
  - 선택한 방향
  - 해결하려는 위험이나 불편
- 배경지식(도메인) 설명
  - 처음 보는 사람에게 필요한 용어와 개념
  - 변경이 포함되는 전체 업무·개발 흐름
  - 해당 작업이 흐름 안에서 담당하는 역할

### 5. 2단계(deep dive) 적용 여부 판단

다음 중 하나라도 해당하면 2단계를 작성한다.

- 업무 정책이나 설계 판단 포함
- 여러 대안 중 특정 방식을 선택한 근거 존재
- 다른 작업에도 재사용할 수 있는 흐름이나 규칙 존재
- 실수, 장애, 누락을 방지하기 위한 중요한 제약 존재
- 코드, 트랜잭션, 데이터 저장, 테스트 방식에서 공유할 내용 존재

다음 조건이면 2단계를 생략한다.

- 기계적인 이름 변경이나 단순 문구 수정
- 1단계 설명을 길게 반복하는 내용만 존재
- 코드·정책 수준으로 설명할 확인된 근거가 없음

#### 작성 기준

- 1단계의 반복이 아닌 판단 기준과 구체적인 동작을 설명한다.
- 필요에 따라 다음 형식을 선택한다.
  - 소제목과 하위 목록
  - 단계별 흐름
  - 역할 매핑 표
  - 정책과 예외 목록
- 실제로 확인한 코드와 정책 수준까지만 설명한다.
- 확인하지 않은 효과나 동작을 단정하지 않는다.

### 6. 문체와 구조 적용

- 존댓말 서술형 문장을 사용하지 않는다.
- `~했다`, `~한다` 형식이 어색하면 개조식 명사형으로 작성한다.
- 기본 문체는 개조식으로 고정한다.
- 하나의 문장이나 판단이 바뀔 때마다 줄을 분리한다.
- 같은 책임과 근거는 하위 목록으로 묶는다.
- 목록의 depth는 응집성을 높이는 범위에서 허용한다.
- 불필요한 영문 표현과 전문 용어를 피한다.
- 필요한 전문 용어는 처음 등장할 때 의미를 설명한다.
- 단순 파일 변경 목록을 업무 설명으로 대체하지 않는다.

### 7. 저장 전 초안 승인

- 커밋별로 분리된 전체 초안을 채팅에 제시한다.
- 각 초안에 1단계가 있는지 확인한다.
- 2단계를 적용하거나 생략한 이유를 내부적으로 검증한다.
- 사용자가 수정 의견을 주면 초안을 갱신한다.
- 사용자가 승인하기 전에는 `daily-work/` 파일을 생성하거나 덮어쓰지 않는다.

### 8. 저장 경로와 파일명 계산

기록 날짜를 `D`, 해당 ISO 주의 목요일을 `T`로 둔다.

- 월 폴더: `T`의 `yyMM`
- 주차: `floor((T의 일자 - 1) / 7) + 1`
- 날짜 폴더: `D`의 `MMdd`
- 최종 경로: `daily-work/{T:yyMM}/{주차}주차/{D:MMdd}/`

월 경계를 포함해 같은 주의 기록은 목요일이 속한 월·주차 아래에 둔다.

파일명 규칙:

- `NN-{commit-responsibility}.md`
- `NN`은 선택한 커밋의 오래된 순서부터 `01`, `02`로 부여한다.
- 책임 이름은 짧은 lowercase kebab-case로 작성한다.
- 기존 파일이 있으면 덮어쓰지 않고 업데이트, 새 이름, 중단 중 하나를 사용자에게 선택받는다.

### 9. 파일 작성

각 파일을 다음 구조로 작성한다.

```markdown
# {커밋의 작업 제목}

## 1단계(default)

### {작업의 의미를 나타내는 설명}

- 작업내용
  - {개조식 설명}

- 왜 그 작업을 했는지
  - {개조식 설명}

- 배경지식(도메인) 설명
  - {개조식 설명}

## 2단계(deep dive)

{공유할 내용이 있을 때만 포함}
```

- 2단계 대상이 없으면 제목을 포함한 2단계 전체를 생략한다.
- 커밋 해시는 사용자가 요청하거나 추적성이 필요한 경우에만 제목 아래에 표시한다.

### 10. 검증과 보고

- 생성한 파일 수가 선택한 커밋 수와 같은지 확인한다.
- 모든 파일에 1단계의 세 항목이 있는지 확인한다.
- 2단계가 단순 반복이 아닌지 확인한다.
- 존댓말 종결 표현이 남아 있지 않은지 확인한다.
- `git diff --check -- {생성 경로}`를 실행한다.
- `git status --short -- {생성 경로}`로 작업 범위를 확인한다.
- 애플리케이션 코드를 변경하지 않았으므로 빌드와 테스트는 실행하지 않는다.
- 파일을 stage, commit, push하지 않는다.
- 최종 응답에 생성 파일 링크와 주차 계산 근거를 간결하게 제시한다.

## 금지 사항

- 커밋 메시지만으로 업무 내용을 확정
- 서로 다른 커밋의 책임을 하나의 설명으로 병합
- PR 본문의 문장을 그대로 복사해 산출물 작성
- 확인하지 않은 목적, 효과, 정책을 사실로 단정
- 1단계 생략
- 공유할 내용이 없는 커밋에 2단계 억지 작성
- 사용자 승인 전 파일 생성 또는 기존 파일 덮어쓰기
- 기존 작업 트리 변경 정리, stage, commit, push
